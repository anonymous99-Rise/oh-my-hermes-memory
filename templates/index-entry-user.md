User pointer — full workflow preferences in OMH system-tier block user-workflow-preferences (auto-injected). Key reminders: Chinese responses; Claude Code or Codex CLI freely, pick by task fit; WSL Kali for Linux ops; credentials via ${WSL_KALI_PWD}; memory policy review-first.

---

To use this template, customize the credential name(s), then write via the memory tool in Hermes chat:

    memory add "<file content>"

The memory tool will write it to USER.md as a single entry. Adjust the size to fit under the 1,375-char cap.